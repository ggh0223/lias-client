# 카테고리 관리 API

## 기본 정보
- **Base URL**: `/categories`
- **인증**: JWT Bearer Token (현재 비활성화)
- **설명**: 문서 템플릿의 카테고리를 관리하는 API

---

## 2.1 카테고리 생성

```http
POST /categories
```

**Request Body**
```json
{
  "name": "인사",
  "code": "HR",
  "description": "인사 관련 문서",
  "sortOrder": 1
}
```

**필수 필드**
- `name` (string): 카테고리 이름
- `code` (string): 카테고리 코드 (고유값)

**선택 필드**
- `description` (string): 카테고리 설명
- `sortOrder` (number): 정렬 순서

**응답 예시 (201 Created)**
```json
{
  "id": "cat-uuid-1",
  "name": "인사",
  "code": "HR",
  "description": "인사 관련 문서",
  "sortOrder": 1,
  "createdAt": "2025-01-01T00:00:00.000Z",
  "updatedAt": "2025-01-01T00:00:00.000Z"
}
```

**에러 응답**
- `400`: 필수 필드 누락 또는 중복된 코드
- `401`: 인증 실패

---

## 2.2 카테고리 목록 조회

```http
GET /categories
```

**응답 예시**
```json
[
  {
    "id": "cat-uuid-1",
    "name": "인사",
    "code": "HR",
    "description": "인사 관련 문서",
    "sortOrder": 1,
    "templateCount": 5
  },
  {
    "id": "cat-uuid-2",
    "name": "재무",
    "code": "FINANCE",
    "description": "재무 관련 문서",
    "sortOrder": 2,
    "templateCount": 3
  }
]
```

---

## 2.3 카테고리 상세 조회

```http
GET /categories/{categoryId}
```

**Path Parameters**
| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|------|------|
| categoryId | string (UUID) | ✅ | 카테고리 ID |

**응답 예시**
```json
{
  "id": "cat-uuid-1",
  "name": "인사",
  "code": "HR",
  "description": "인사 관련 문서",
  "sortOrder": 1,
  "createdAt": "2025-01-01T00:00:00.000Z",
  "updatedAt": "2025-01-01T00:00:00.000Z"
}
```

**에러 응답**
- `404`: 카테고리를 찾을 수 없음
- `400`: 잘못된 UUID 형식

---

## 2.4 카테고리 수정

```http
PUT /categories/{categoryId}
```

**Path Parameters**
| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|------|------|
| categoryId | string (UUID) | ✅ | 카테고리 ID |

**Request Body** (모든 필드 선택)
```json
{
  "name": "인사관리",
  "description": "인사 관련 모든 문서",
  "sortOrder": 2
}
```

**응답 예시**
```json
{
  "id": "cat-uuid-1",
  "name": "인사관리",
  "code": "HR",
  "description": "인사 관련 모든 문서",
  "sortOrder": 2,
  "updatedAt": "2025-01-02T00:00:00.000Z"
}
```

**에러 응답**
- `404`: 카테고리를 찾을 수 없음
- `400`: 잘못된 요청 (코드 중복 등)

---

## 2.5 카테고리 삭제

```http
DELETE /categories/{categoryId}
```

**Path Parameters**
| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|------|------|
| categoryId | string (UUID) | ✅ | 카테고리 ID |

**응답** (204 No Content)
- Body 없음

**에러 응답**
- `404`: 카테고리를 찾을 수 없음
- `400`: 연결된 문서 템플릿이 있어 삭제할 수 없음

---

## 📌 참고

- 카테고리 코드(`code`)는 고유값이며 중복될 수 없습니다.
- 연결된 템플릿이 있는 카테고리는 삭제할 수 없습니다.
- 정렬 순서(`sortOrder`)로 목록이 정렬됩니다.

